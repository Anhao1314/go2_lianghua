# 第一阶段修复验证

代码提交：567b3ba2b898cbfa9da8af94670dba139cc63233。基准：1ee380c35915fb1798f6a0d5f25f3f8dbc243cf1。

## 验证结果

- 全量测试：265 passed；新增可靠性测试 18 项，补充的失败后重试场景也通过。
- bash -n、Python 编译与 git diff --check：通过。
- 公开跟踪文本扫描：未发现旧 Webhook、个人训练路径、原局域网地址或个人助手路径。
- Markdown 本地文件链接 5 项，无失效路径。
- 风险、筛选、价格配置保持原值；factors.py 与 schema.py 未改动。
- 人工标签重复键、非法值拒绝；原始自动数据的异常仍由质量检查诊断。
- 单 CSV 写入失败保持旧文件；定时采集/管线失败不写日期标记、不发布，下一次成功能够重试。
- 同步保护测试使用临时本地 bare 仓库；不连接 GitHub，验证错误分支、已有暂存、非数据改动及远端领先会停止。

## 离线回归

复制已提交 datasets 至隔离工作目录，并显式配置输入路径，输出到本目录。未连接训练面板、未发送消息、未安装定时任务或推送。

输入 SHA-256 见 input_checksums.json。输出日期 2026-09-06 仅用于文件名，不截断数据。

- 48 runs，分类有效标签 pass 12 / fail 23。
- 事后分类 LOO accuracy 0.943；包含最终验收特征，不证明在线预测能力。
- success_rate 回归：有效样本 35，R² -123.221，MAE 1.6；Dummy MAE 0.4。
- duration_seconds 回归：有效样本 6，R² -10.103，MAE 1368361.8 秒；Dummy MAE 1037075.6 秒。
- 两个回归结果均不足以用于决策。duration 为快照跨度，不能当作实际计算时长。

基线报告中的 dirty 标记包含未提交的回归产物，不表示未记录的算法修改。历史报告没有覆盖；runs.csv 仅将一个个人源路径脱敏，数据指标未改。

## 尚未验证及兼容变化

- 原生 Windows 实际执行、真实 Linux 采集与 flock 并发未验证；Shell 失败场景通过模拟 Python/锁命令验证。
- --test-notify 与飞书通知配置已移除；监控仍保留本地建议、表格、日志与原进度估算。
- 本机部署需创建 config.local.json；无 --config 时优先本地配置。公开默认配置不含采集源。
- 单文件替换不是多表事务；run_id、整批发布和在线预测截面留待下一阶段。
- 本轮未重写历史、未推送。历史已公开 Webhook 需由所有者在飞书侧撤销或重置。
