# go2w-quant 基线模型报告

- 数据集: `dataset_2026-08-17.csv`（24 runs，28 特征）
- 生成时间: 2026-08-17

> 当前样本量远低于可训练门槛，结果只作管线验证与基线记录，不作为训练决策依据。

## 一、verdict 分类（验收早停）

- 标签分布：pass 0 / fail 1
- 样本不足（门槛：正负样本各 ≥2），未训练模型。

## 二、回归（success_rate / duration_seconds）

### success_rate（有效样本 0）
- 样本不足（门槛：≥6），未训练模型。

### duration_seconds（有效样本 17）
- LOO 线性回归：R2 0.402，MAE 55672.8
- 中位数 Dummy：MAE 78573.5
- 使用特征数：27；重要特征：snapshot_count(71197.51), swap_percent_max(56366.174), timesteps_growth(23239.441), stall_minutes(20357.127), eval_peak_reward(16846.778), eval_std_recent(15925.536), approx_kl_last(12186.933), reward_peak_ratio(10906.935)

## 三、单变量相关性 Top（信息性参考）

### verdict
- 无可计算相关性的特征。

### success_rate
- 无可计算相关性的特征。

### duration_seconds
- time_span_minutes: r=1.0（n=17）
- swap_percent_max: r=0.973（n=17）
- eval_peak_reward: r=0.946（n=5）
- mem_percent_max: r=0.911（n=17）
- snapshot_count: r=0.906（n=17）
- cpu_percent_max: r=0.864（n=17）
- progress_ratio: r=-0.729（n=4）
- stall_minutes: r=0.596（n=17）